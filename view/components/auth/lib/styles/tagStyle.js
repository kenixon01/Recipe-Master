import { Dimensions, StyleSheet } from "react-native";

export default StyleSheet.create({
    tag:{
        padding: 7,
        margin: 5,
        borderRadius: 100,
      },
      text:{
        fontSize: 15,
      },
})